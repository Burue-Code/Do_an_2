# Database – Movie Recommendation System

Thư mục database theo tech stack (monorepo).

## Migration
Migration chính nằm trong `backend/src/main/resources/db/migration/` (Flyway).

## Thư mục này
- Dùng cho script backup, restore, seed bổ sung
- Schema tham khảo: `docs/database/`
